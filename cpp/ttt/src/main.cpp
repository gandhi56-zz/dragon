
#include "../include/server.h"

int main(){
	Server server;
	server.run();

	//State state;
	//state.set_size(3, 3);
	//state.create_graph();

	//state.update(Action("Wa3Wc1Wb2"));
	//state.show();
	return 0;
}
